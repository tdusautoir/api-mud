export enum MudStatusCode {
    OK = 200,
    CREATED = 201,
    BAD_REQUEST = 400,
    UNAUTHORIZED = 401,
    FORBIDDEN = 403,
    NOT_FOUND = 404,
    INTERNAL_SERVER_ERROR = 500
}

const RANK_KEY = "RANK";
const RANK_BAD = "BAD";
const RANK_AVERAGE = "AVERAGE";
const RANK_GOOD = "GOOD";
const RANK_GOD = "GOD";

export {
    RANK_KEY,
    RANK_BAD,
    RANK_AVERAGE,
    RANK_GOOD,
    RANK_GOD
}
